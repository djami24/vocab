rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Har bir foydalanuvchi FAQAT o'zining hujjatini o'qiy va yoza oladi.
    match /users/{userId} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
  }
}
